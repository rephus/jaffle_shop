version: 2
models:
  - name: write_back_sql
    label: Write back sql
    description: SQL model for friendlyName(write back sql)
    columns:
      - name: order_id
        meta:
          dimension:
            type: number
      - name: order_date
        meta:
          dimension:
            type: date
      - name: status
        meta:
          dimension:
            type: string
